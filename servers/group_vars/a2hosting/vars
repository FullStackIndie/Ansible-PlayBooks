ansible_port: "{{ vault_server1_ssh_port }}"
ansible_user: "{{ vault_server1_ssh_user }}"
ansible_pass: "{{ vault_server1_ssh_pass }}"
ansible_become_password: "{{ vault_server1_become_password }}"
server1_ip: "{{ vault_server1_ip }}"
docker_username: "{{ vault_docker_username }}"
docker_password: "{{ vault_docker_password }}"
crowdsec_metabase_password: "{{ vault_crowdsec_metabase_password }}"
fullstackindie_admin_dashboard_password: "{{ vault_fullstackindie_admin_dashboard_password }}"